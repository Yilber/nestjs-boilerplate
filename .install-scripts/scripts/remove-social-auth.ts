// import replace from '../helpers/replace';
// import path from 'path';

const removeSocialAuth = async () => {
  // replace({
  //   path: path.join(process.cwd(), 'src', 'users', 'domain', 'user.ts'),
  //   actions: [
  //     {
  //       find: /\/\/ <database-social-block>.*\/\/ <\/database-social-block>/gs,
  //       replace: '',
  //     },
  //   ],
  // });
};

export default removeSocialAuth;
